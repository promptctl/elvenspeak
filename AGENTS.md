# AGENTS

## Never build an image from a working tree

Before you touch anything that builds an image or deploys this service, open CLAUDE.md in this repo and read the section under this same heading — what is banned, why, and the procedure to use instead. Take the rule from there, not from memory and not from this file.

This file deliberately carries no version of that rule. When it did, the two statements had already drifted apart within a single commit, and a reader confronted with two wordings has no way to tell which one is live.

<!-- BEGIN LIT INTEGRATION -->
## lit Agent-Native Workflow

This repository uses `lit` for agent-native issue tracking.

Start by running `lit quickstart` to load the workflow instructions. It prints how tickets are found, created, updated, and closed here, so running it first means the rest of your work follows the conventions this repo expects. It's a quick, read-only command — no need to check in before running it.

<!-- END LIT INTEGRATION -->
